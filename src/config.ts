
/* MAIN */

const DEFAULTS = {
  id: 'confId',
  scope: 'provider',
  indentation: '\t',
  defaults: {},
  defaultsRaw: '{\n\t\n}'
};

const SCOPE_ALL = '*';

const SCOPE_DEFAULTS = 'defaults';

/* EXPORT */

export {DEFAULTS, SCOPE_ALL, SCOPE_DEFAULTS};
