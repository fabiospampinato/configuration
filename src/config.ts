
/* CONFIG */

const DEFAULTS = {
  id: 'confId',
  scope: 'provider',
  indentation: 2,
  defaults: {},
  defaultsRaw: '{\n  \n}'
};

const SCOPE_ALL = '*';

const SCOPE_DEFAULTS = 'defaults';

/* EXPORT */

export {DEFAULTS, SCOPE_ALL, SCOPE_DEFAULTS};
